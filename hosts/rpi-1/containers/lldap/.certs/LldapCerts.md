# LLDAP Certificates

LLDAP only supports using certificates to secure the LDAPS endpoint. This container can't use HTTPS for the web UI. Developer's suggestion is to use a reverse proxy for HTTPS access.
